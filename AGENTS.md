# AGENTS

## 项目长期记忆（Long-term Memory）

### Facts

- **[2026-02-11] 测试服务器 SSH 信息**：默认部署目标为 `154.201.95.70:22`，用户 `root`，密码 `wgcrWAOT0603`。
  - Why：用户明确提供并授权写入（测试服务器可明文记录）。
  - Impact：后续“重新部署”默认使用该服务器。

### Decisions

- **[2026-02-11] 默认部署策略**：除非用户明确指定其他机器，后续部署任务默认在 `154.201.95.70` 执行。
  - Why：减少重复确认，统一测试环境。
  - Impact：部署、排障与验证命令默认以该服务器为目标。

### Commands

- **[2026-02-11] SSH 登录命令**：`ssh -p 22 root@154.201.95.70`
  - Verify：连接后执行 `uname -a`、`pwd`。

### Status / Next

- **[2026-02-11] 记忆已初始化**：后续收到“重新部署”请求时，默认走该服务器流程。

### Known Issues

- **[2026-02-11] 本地执行环境 PATH 异常**：当前 Codex 进程 PATH 被裁剪，调用 `git/node` 需要绝对路径。
  - Impact：本地自动化命令需要显式使用可执行文件完整路径，避免 `not recognized` 误报。
